#include "question1.h"
#include <cstdlib>
#include <ctime>

// Function to test if the configuration is working correctly
bool test_config() {
    return true;
}

// Function to roll a die and return a random value between 1 and 6
int roll_die() {
    return (rand() % 6) + 1;
}
